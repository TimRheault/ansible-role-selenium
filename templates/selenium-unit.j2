[Unit]
Description=selenium test framework
After=syslog.target network.target

[Service]
ExecStart=/usr/bin/java -jar {{ selenium_install_dir }}/selenium/selenium-server-standalone-3.8.1.jar -role hub
Restart=on-failure
RestartSec=20s

[Install]
WantedBy=multi-user.target
